# TODO — Yappanese N5 判定テスト

最終更新：2026-08-27

このファイルは、会話や作業の中で「後でやる」と判断した項目を残すためのもの。
リポジトリに置いてあるので、開発中に必ず目に入る。
新しく先送りにした項目は、その場でここに追記する。

---

## 実運用の前に必ず対応（未対応のまま公開しない）

- [ ] **Vercel の2段階認証を設定する**
      2026-08-27 にセットアップ画面でスキップした。
      このアカウントは学生の個人情報と Supabase の管理者キーに触れる。

- [ ] **Supabase の2段階認証を設定する**
      同じ理由。

- [ ] **個人情報の取扱い方針を定める**
      現在、学生の氏名・国籍・生年月日・試験結果を保持している。
      個人情報保護法上、以下が必要。
      - 利用目的の明示（受験画面に表示する）
      - 保管期間の設定と、期間経過後の削除
      - 学生本人からの開示・削除の求めへの対応方法
      - 第三者（入管）への提供についての説明

- [ ] **問題30問を笈川先生に監修していただく**
      現在の問題は Claude が JLPT N5 の形式・難易度に準拠して作成したもの。
      公式問題ではない。文法問題は sentence_patterns.json の N5 35文型に対応させている。
      記録書には自作ツールである旨を印字しているが、内容の妥当性は先生の確認が必要。

- [ ] **PWA のアイコンを正式なものに差し替える**
      現在は Python で生成した仮のもの（「N5」の文字を図形で描いたもの）。

- [ ] **記録書の様式を入管の「別添2 各種確認書（参考様式）」と突き合わせる**
      現在は入管が求める記載項目（使用ツール・出題数・正答数・合否基準）を
      満たす形で独自に作成している。
      参考様式には試験日・試験場所・受験番号・既学習時間の項目があるため、
      実物と照合して過不足を確認する。

---

## 常に守る

- [ ] **service_role キーをブラウザから見える場所に置かない**
      config.js に書いてよいのは publishable（anon）キーのみ。
      service_role は RLS を無視するため、漏れると全校のデータにアクセスできる。
      Vercel の環境変数に置き、サーバー側の処理からのみ使う。

---

## Bの開発（実運用形）— 進行中

- [x] Vercel へ移行（GitHub Pages ではサーバー側の処理が置けないため）
- [ ] **学校の招待機能**（我々が学校を登録し、招待リンクを送る）
- [ ] **職員の招待機能**（学校が自分で担当者を追加できる）
- [ ] 機関情報の編集（学校名・住所を管理画面から変更）
- [ ] 受験者の管理（重複の統合・削除）
- [ ] 我々用の管理画面（全機関の一覧、学校の登録・招待）

---

## アプリ本体（Realtime API 版）

- [ ] **低速・不安定な回線での検証**
      要件定義書は東南アジア郊外での利用を前提としている。
      WebRTC で音声を常時ストリーミングするため、
      3層構成より回線品質への依存が強い。実測が必要。

- [ ] **PC のマイクが動かない問題**
      2026-08-25 に Realtime の動作確認で詰まった。
      session 200 / calls 201 まで通っており、実装の問題ではない。
      Chrome のマイク許可設定を確認する。

- [ ] **speech_patterns.json の service_counter 型を検証する**
      接客の型は、笈川先生の実授業データに根拠がない（42本に接客場面がない）。
      業務内容から設計したものであり `verified: false` としている。
      コンビニ各社への確認、または店舗での実地調査が必要。

- [ ] 学習履歴の永続化（現在 Session はメモリ上のみ）
- [ ] 間隔反復の出題キュー（next_interval_days は計算するが管理は未実装）
- [ ] 録画機能（topics.json の record: true が対象・30件）

---

## 定義ファイルの未確定事項

- [ ] **入力がない場合にお題を繰り返すまでの時間**
      behavior_policy §7-3。実授業からは測定できないため A/B テストで決める。

- [ ] **アバターの褒め方を状態で切り替える軸**
      behavior_policy §4-1。性別で変えることは禁止した。
      状態（発話量が伸びた／詰まったが完走した等）で変える案があるが、
      実データに根拠がないため設計判断になる。

- [ ] **ピアフィードバックをアプリでどう実現するか**
      behavior_policy 付録D-1。
      先生の発話シェアを最も強く決めていたのはピアフィードバックの回数だった。
      人間同士を接続するか、AI が「教室の他者」を演じるか。
      後者は実装が軽いが、価値が「相手が実在すること」に依存する場合は機能しない。

- [ ] **間隔反復の係数を A/B テストで検証する**
      repetition_protocol.json の ratio_table は Cepeda 2008 の値。
      研究は事実の記憶が対象であり、会話スキルへの外挿は未検証。

- [ ] **「訂正しない」判断の基準**
      先生は278回訂正しているが、触れなかった誤りの基準は
      42本のデータからは抽出できなかった（分母が測れない）。
      先生への確認事項。

---

## 測定できないと確定した項目（同じ手法では今後も測れない）

behavior_policy 付録B に記載。次に動画を追加しても以下は測れない。

- 沈黙の秒数・介入までの時間（Whisper のタイムスタンプが連続しているため）
- 学習者の音読の質（一斉音読中はミュートで録音に存在しない）
- 1回のフィードバックの効果（対照群がなく、発話量は先生の指名で決まる）
- 視線・表情（音声データに存在しない）

---

## 保留と判断した項目

- **残り151本の文字起こし**
  規則として抽出すべきものは42本で出尽くしている。
  事例は3倍に増えるが、先生に直接聞く方が速く狙った場面が取れる。
  教師研修35本だけは「なぜそうするか」を先生が言語化している唯一の素材のため、
  必要になれば優先する。

- **日本語学校の買収**
  留学課程は校地・校舎の自己所有が必須で、認定の通過率は30〜40%。
  就労課程でも施設要件は同じ。
  1校あたりの投下資本利益率は9〜14%で、ロールアップは成立しなかった。
  認定校との提携で目的（実証・流路）は達せられる。

- **建設・港湾分野**
  職業安定法により有料職業紹介の対象外。無料紹介なら可能だが収益にならない。
